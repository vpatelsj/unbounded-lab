# W1.2 vLLM sanity check — Qwen3-30B-A3B-GPTQ-Int4 on `spark-2c24`

**Status:** complete for the placeholder-tuning baseline. The deployment
([`../../inference/vllm-qwen-moe/`](../../inference/vllm-qwen-moe/)) is shipped
and serving; the W1.6 benchmark harness has been run end-to-end against
it and the throughput sweep is recorded below. Re-runs after the GB10
MoE Triton tune lands and after vLLM ships sm_121a-aware kernels are
both still pending — see the Results table.

> **Plan deviation — model variant.** The plan calls for BF16. Deployed
> variant is `Qwen/Qwen3-30B-A3B-GPTQ-Int4` because (a) ~65 GiB of
> unified memory on `spark-2c24` is pinned in unreclaimable host page cache
> (DGX OS denies `drop_caches`) and (b) vLLM v0.11's prebuilt CUTLASS
> scaled-mm has no sm_121a kernel, so FP8 fails before profile_run
> finishes. GPTQ-Int4 dispatches to Marlin and works. Deviation chain is
> documented in [`../../JOURNAL.md`](../../JOURNAL.md) and
> [`../../inference/vllm-qwen-moe/README.md`](../../inference/vllm-qwen-moe/README.md).

The plan flags this deployment as **on the edge of OOM for KV cache at 32K**. The
purpose of this sanity check is to replace that hand-wave with measured numbers
**before** anything in Wave 2 builds on top of it. With GPTQ-Int4 weights
(~15.6 GiB GPU footprint) plus the unreclaimable host cache, the working KV
pool is 9.31 GiB / 101 712 tokens — 3.10x concurrency at the configured 32K
max-model-len.

## What gets measured

Three scalar dimensions, swept independently against the deployed config (single
GB10, GPTQ-Int4, `--max-model-len 32768`, `--gpu-memory-utilization 0.22`,
`--tensor-parallel-size 1`, `--enforce-eager`, `VLLM_USE_FLASHINFER_SAMPLER=0`):

1. **Sustainable concurrent requests (batch).** Smallest count at which p99 TTFT
   stops being flat — the saturation point, not the OOM point.
2. **Sustainable context length.** Largest prompt length the server accepts at
   batch=1 without KV-cache eviction or `--max-model-len` rejection. The plan's
   "edge of OOM at 32K" claim gets a number here.
3. **Sustainable prompt throughput.** Tokens/s in and tokens/s out, at the
   saturation point from (1).

All runs hit `vllm.lab-vllm-qwen-moe.svc.cluster.local:8000/v1/completions` from
an in-cluster pod, so the path under measurement excludes the Ollama-compat
sidecar (that has its own row).

## Methodology

W1.6 ships the harness ([`../../bench/lab_bench.py`](../../bench/lab_bench.py));
W1.2 consumes it. Run from in-cluster as a Job so traffic crosses the
same WireGuard path real clients use:

```sh
make w1.6-run-vllm                 # runs bench/job-vllm-w1.2.yaml end-to-end
make w1.6-results-fetch            # extracts JSON to bench/results/
```

The Job pins to the AKS gateway/system pool (amd64), targets the
in-cluster `vllm.lab-vllm-qwen-moe.svc:8000/v1` endpoint, sweeps
concurrency `[1, 4, 8, 16]`, runs 20 measured requests per phase with
3 discarded warmups, and emits a single JSON file. Per the plan's
benchmark methodology, the JSON records prompt length, generation
length, concurrency, model, engine, image tag, and timestamps; bare
numbers without context are deliberately not retained.

## Results

Append rows as runs land. **Do not delete previous rows** — they are the
before/after record for the MoE tuning JSON work and any future model-version bump.

| Date | vLLM image | Tuning JSON state | Concurrency at p99 TTFT knee | Max accepted context (B=1) | Tokens/s in (sat) | Tokens/s out (sat) | Notes |
|---|---|---|---|---|---|---|---|
| 2026-04-30 | `vllm/vllm-openai:v0.11.0` | placeholder `{}` | knee not reached at c=16 | 32 768 (KV pool 101 712 tokens / 3.10x at 32K) | ~6 580 prompt tok/s aggregate at c=16 (411 prompt tok x 16 in 5.54 s wallclock) | **62.5 t/s @ c=1; 199.6 t/s @ c=4; 299.2 t/s @ c=8; 462.2 t/s @ c=16** (aggregate decode); per-request decode degrades 62 -> 45 t/s as concurrency rises | 100% success rate across all 80 measured requests. p50 latency 2.05 s @ c=1, 2.79 s @ c=16 (3.5x throughput for 1.4x latency cost). `--enforce-eager`, FlashInfer sampler off. KV pool 9.31 GiB at `--gpu-memory-utilization=0.22`. Source: [`../bench/results/lab-bench-vllm-w1-2.json`](../bench/results/lab-bench-vllm-w1-2.json). |
| _pending_ | `vllm/vllm-openai:v0.11.0` | tuned for E,N,GB10 | TODO | TODO | TODO | TODO | Re-run after the GB10 MoE Triton tune lands |
| _pending_ | _GB10-aware build_ | placeholder `{}` | TODO | TODO | TODO | TODO | Re-run after vLLM ships sm_121a-aware CUTLASS / FlashInfer wheels; expect lift from `--enforce-eager` removal alone |

## Cross-reference

- Cold-start / egress / disk-footprint numbers go to
  [`../../JOURNAL.md`](../../JOURNAL.md), not here. This page is
  steady-state throughput only.
- W1.1 Ollama equivalent numbers (already captured): see the Ollama rows in the
  storage-pain journal; throughput numbers there (77.4 t/s warm) are
  Ollama+GGUF Q4_K_M and are **not directly** comparable to the GPTQ-Int4 vLLM
  numbers this page will hold — different quantization, different kernel
  family (llama.cpp vs Marlin), different sampler stack. The W1.3 dedup-pain
  comparison cares about *bytes on disk* and *bytes pulled*, not t/s.
