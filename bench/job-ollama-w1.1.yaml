apiVersion: batch/v1
kind: Job
metadata:
  name: lab-bench-ollama-w1-1
  namespace: lab-bench
  labels:
    app.kubernetes.io/name: lab-bench
    lab.unbounded.cloud/wave: "1.1"
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: lab-bench
    spec:
      restartPolicy: Never
      securityContext:
        fsGroup: 65532
        runAsNonRoot: true
      nodeSelector:
        kubernetes.io/os: linux
        kubernetes.io/arch: amd64
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: agentpool
                    operator: In
                    values: ["gwmain", "system"]
      enableServiceLinks: false
      containers:
        - name: bench
          image: python:3.12-slim
          imagePullPolicy: IfNotPresent
          command: ["python", "/bench/lab_bench.py"]
          args:
            - --engine=ollama
            - --url=http://ollama.lab-ollama-qwen-moe.svc.cluster.local:11434
            - --model=qwen3:30b-a3b-q4_K_M
            - --prompt-len=512
            - --gen-len=128
            - --concurrency=1,4
            - --runs=20
            - --warmup=3
            - --prom-url=http://lab-obs-kube-prometheus-st-prometheus.lab-observability.svc.cluster.local:9090
            - --label=w1.1-baseline-ollama
            - --out=/results/w1.1-ollama.json
          volumeMounts:
            - { name: bench-script, mountPath: /bench, readOnly: true }
            - { name: results,      mountPath: /results }
          resources:
            requests: { cpu: "200m", memory: "256Mi" }
            limits:   { cpu: "1",    memory: "512Mi" }
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 65532
            capabilities: { drop: ["ALL"] }
            seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: bench-script
          configMap:
            name: lab-bench-script
            items:
              - { key: lab_bench.py, path: lab_bench.py }
        - name: results
          persistentVolumeClaim:
            claimName: lab-bench-results
